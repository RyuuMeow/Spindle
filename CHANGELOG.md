# 版本記錄

## 0.8.0 — 2026-09-20

- 圖表採一次性 ELK 分層排版與 libavoid 直角避障；首次無布局才自動整理，之後由使用者整理全部或所選節點。
- 同組條件／選項保留獨立轉場、原文順序與父路徑，卡片成欄、共用幹線分岔；中心接點、pin 與手動幾何跟隨視圖保存。
- 左鍵框選、Shift 多選及整組搬移；右鍵移動超過 5px 才平移，單擊開啟物件選單。線路／條件卡不自動打開側欄。
- 雙擊線段新增 pin，可拖動 pin、線段、幹線與條件卡；Delete 只刪選取 pin，Esc 取消，提供簡化／恢復自動線路。
- 完整布局 Undo／Redo 保留 50 筆；來源交易映射讓插入台詞、改名及切換模式不錯配手動布局。舊布局保留節點座標與視野。
- pin 拖動不留下重複折返；修線同步核對條件卡通道與手動共用幹線。
- Worker、WASM 與引擎授權檔離線打包；失敗或無解保留幾何並提供重試。縮放只隱藏摘要，不改變節點尺寸。

## 0.7.1 — 2026-09-20

- 圖表保留有效路徑，僅重算端點或障礙確實改變的線；分支埠順序不隨相鄰節點拖動交換，返回線不依賴全圖最高點。
- 窄通道縮小避讓留白；標籤在主路徑完成後布局，擁擠處使用短引線，避免為了文字繞遠。
- 劇本與資料夾採整列指標拖移，5px 起拖門檻、白色落點線、放開提交及 Esc 取消；移除根目錄拖移文案，命名其他物件不再停用整棵樹。
- 無問題的檢查按鈕顯示勾號；錯誤／警告組間距增加至 12px。

## 0.7.0 — 2026-09-20

- 閱讀大綱、搜尋與跨檔定位使用同一來源位置請求；定位到已收合場景時自動展開。
- 新增純閱讀 toggle，隱藏命令、tags 與技術資訊；作者統計改為右側面板，提供角色／場景台詞分布與定位。
- 文件與資料夾共用白色直線排序提示、跨層拖移、行內命名與右鍵動作。資料夾僅箭頭展開／收合，單擊選取、雙擊改名；空資料夾與混合順序可保存。
- 移除「從專案移除」入口；刪除先送垃圾桶。複製劇本／場景直接建立並改名；場景移動選目的清單，刪除可 Undo。
- 頂列提示向下避開原生 caption；診斷按鈕分別顯示紅色錯誤與黃色警告，專案選單保留一致圖示欄。
- 流程圖依前進方向分層，返回線走外側通道，分支埠與標籤寬度分配減少交疊。連線／條件點擊僅選取；既有手排布局須按「自動整理」套用新布局。

## 0.6.5 — 2026-09-20

- 指令自動參數提示只顯示空位置；讀取完整來源，已有值與未完成輸入不反覆彈窗。
- 純文字、閱讀與節點共用補全 → 空參數 → hover 優先順序，同時最多一個指令提示。
- 補齊 hover 取代、Esc、失焦與候選清單互斥的桌面驗證。

## 0.6.4 — 2026-09-20

- 劇本移到垃圾桶不再要求確認；磁碟檔案使用 Windows 資源回收筒，刪除前保留 App 復原副本，失敗不改為永久刪除。
- 新增／更名劇本與場景、套用指令定義移除轉圈；保留重複提交防護與錯誤提示。
- 閱讀或節點修改後，切回純文字立即同步保留的 Monaco model；首筆輸入與共用 Undo／Redo 使用最新內容。
- 14 個支援的內建指令補齊語法、參數型別／說明與範例；條件、指定值按完整運算式提示，不被空格誤分成多個參數。
- 純文字、閱讀與節點支援跨劇本變數補全；`set` 自動提供可寫變數，運算式／條件輸入 ` 查找定義，避免字串／註解誤觸發及重複 `。候選清單開啟時暫停參數浮窗，避免遮住選項。

## 0.6.3 — 2026-09-20

- 閱讀模式逐行保留原文空白；已有空白時減少重複段落間距，編輯前後高度穩定。
- 新增劇本確認後留在目前資料夾文件列表頂部；保留既有可見順序並切回手動排序。雙擊劇本原位改名，選取主檔名。
- 補全清單統一有效字型、29px 列高及參數摘要；取消純文字模式重複的說明箭頭與 hover 截斷。
- 純文字指令／參數提示使用 Monaco 原生來源定位，避免依舊布局手算浮窗位置。

## 0.6.2 — 2026-09-19

- 結構檢查按鈕隨數量撐寬，圖示、badge 與點擊範圍保持一致。
- 三種編輯入口統一指令輸入：`<<` 自動配對及開清單、Enter／Tab 補全、目前位置參數提示、跳過既有 `>>`、空配對刪除；貼上維持原文。
- 補全清單使用灰色選取、SVG 圖示及參數摘要，純文字新增選取項目的說明區；指令／參數浮窗共用分層排版。
- 閱讀及節點活動行完整顯示原始 Yarn 與純文字語法配色，離開後恢復渲染；模式切換不改原文。

## 0.6.1 — 2026-09-19

- 指令名稱完成後停止名稱補全，純文字模式顯示目前位置的參數提示；未閉合字串及括號中的空格保持原參數位置。
- 閱讀與節點編輯器的指令候選也限制在名稱輸入範圍；保留節點角色補全及 jump 場景候選。
- 閱讀模式摺疊箭頭對齊標題文字行，排除標題前留白；展開與收合一致。


## 0.6.0 — 2026-09-19

- App 改名 Spindle，使用入庫的 clean／flat SVG；同步 Windows 圖示、portable 啟動畫面、favicon 與關於頁，保留舊資料與安裝識別。
- 純文字、閱讀及節點編輯：Ctrl 懸浮靜態跳轉目標呈現連結，放開即消失；左鍵精確命中才跳轉，未解析與重名目標不冒充連結。
- 指令及參數提示改用標題、說明、對齊參數與範例，來源編輯器採安全 Markdown 表格。

## 0.5.1 — 2026-09-19

- 自訂指令側欄改稱「指令列表」，同稿分頁移除撰寫序號。
- 自動保存移除日常圓點、轉圈與成功狀態，避免閃動及 tab 寬度跳動；保存異常持續顯示。
- 關閉視窗先完成 renderer 交易與工作階段保存；待保存時顯示輕量進度，組字或保存失敗保留視窗。

## 0.5.0 — 2026-09-19

- 自訂指令改為設定式工具 tab，選填標籤、單一搜尋焦點框及較少重複說明。
- 指令與參數新增顯示名稱，參數新增說明；舊定義維持相容。
- 純文字、閱讀與節點編輯支援來源不變的虛擬參數名稱及懸浮說明。
- 點流程圖空白處結束節點編輯；組字、改名與未處理草稿受保護。

## 0.4.0 — 2026-09-19

- 自訂指令改為壓暗背景的獨立面板；搜尋改為中央偏上的即時結果浮層，保護焦點與背景快捷鍵。
- 一般導航保留目前分頁，新增／更名劇本和場景使用行內輸入；排序方向及大綱開關回饋一致。
- 閱讀以正文為主，語義間距、分隔線留白及條件內距對齊，附屬資訊降低視覺重量。
- 節點直接編輯連續來源，支援跨檔、共享 Undo／Redo、來源範圍保護與未提交草稿取回。
- 面板增加表面層次和鍵盤可操作的調整手柄，診斷篩選與參數選單統一樣式。
- 修正新增檔案寫入中斷留下殘檔而無法重試的問題。
- 修正獨立執行個體工作區已載入但視窗仍隱藏的啟動問題。

## 0.3.0 — 2026-09-18

- 重設文件工具列、左右導航及全頁設定／指令工作區，刪除重複標題、常駐統計列及一般保存按鈕。
- SVG 操作／狀態／折疊／渲染圖示，模式、閱讀寬度及歷史呈現共用可鍵盤操作的分段單選。
- 持續全文搜尋、同一滑鼠／鍵盤定位規則，保存查詢與結果位置；數字偏好不再搶改未完成輸入。
- 文件版本歷史、唯讀預覽及差異比較；還原前保留目前內容並檢查預覽版本，最近刪除獨立預覽。
- 閱讀欄寬及字級層級、緊湊命令／選項／條件、精確來源揭露、語境變數標籤及折疊狀態保存；純文字編輯器外觀不變。
- 精簡圖表節點與跨檔入口，保留完整條件祖先，改善往返路線、查找及初次fit；詳情按需開啟。

## 0.2.1 — 2026-09-17

- Portable 解壓期間顯示啟動提示，避免長時間沒有回饋。
- 改用 ZIP 壓縮與每次啟動獨立的暫存目錄，避免重複啟動共用解壓資源。
- 增加實際 DPI、兩個隔離執行個體、重複啟動喚回視窗與資源保留的回歸測試。

## 0.2.0 — 2026-09-17

- Windows 磁碟專案與原檔自動保存，含外部衝突、唯讀、缺檔、復原快照與最近刪除。
- 新增 CodeMirror 即時渲染，整份 Yarn 連續編輯；純文字保留 Monaco。原文、渲染與多視窗共用內容及撤銷紀錄。
- 分頁排序／右鍵／固定／批次關閉／重開、輕量新分頁選擇器、多視窗轉移及工作階段恢復。
- 全專案搜尋、來源返回、場景引用更新、跨檔交易、圖表布局 Undo／Redo 及指令草稿恢復。
- 補齊獨立布局驗證、損毀救援、BOM／CRLF 來源映射及儲存錯誤狀態。
- Windows x64 安裝版與 portable 包含離線編輯器；實測及原生 OS 驗證限制見 [實作追蹤](docs/implementation-progress.md)。

## 0.1.1

整合 Windows 頂欄與分頁、調整加號位置、統一浮層與控制項、改善故事圖表與空態。歷史取證見 [UI 規範](docs/ui-design.md)。
